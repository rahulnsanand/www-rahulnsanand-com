import type { MetadataRoute } from "next";

/**
 * Auto-generated by scripts/generate-robots.mjs.
 * Non-blog page text snapshot:
 * /: "@/components/homepage"; Home()
 * /about: "next"; metadata: Metadata = ; AboutPage()
 * /contact: "next"; metadata: Metadata = ; ContactPage()
 * /projects: "next"; metadata: Metadata = ; ProjectsPage()
 */
export default function robots(): MetadataRoute.Robots {
  return {
    rules: [{ userAgent: "*", allow: ["/","/about","/contact","/projects"] }],
    sitemap: "https://www.rahulnsanand.com/sitemap.xml",
  };
}
